from setuptools import setup, find_packages

setup(
    name="lse",
    version="0.0.1",
    description="Lightweight Self-Evolve agents and tasks",
    packages=find_packages(),
    python_requires=">=3.10",
)

